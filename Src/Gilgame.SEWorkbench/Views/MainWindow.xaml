<Window xmlns:CodeCompletion="clr-namespace:ICSharpCode.CodeCompletion;assembly=ICSharpCode.CodeCompletion"  xmlns:Views="clr-namespace:Gilgame.SEWorkbench.Views"  x:Class="Gilgame.SEWorkbench.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="400" Width="600" FontSize="11">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Gilgame.SEWorkbench;component/Styles/TabControl.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <LinearGradientBrush x:Key="MainMenuBackground" EndPoint="0,1">
                <GradientStop Color="#CAD3E2" Offset="0.0" />
                <GradientStop Color="#AEB9CD" Offset="1" />
            </LinearGradientBrush>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Background="#FF2B3B58">
        <Grid.RowDefinitions>
            <RowDefinition Height="22" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="175" MinWidth="100"/>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" MinWidth="100"/>
        </Grid.ColumnDefinitions>
        
        <Menu Name="MainMenu" Height="22" VerticalAlignment="Top" Background="{StaticResource MainMenuBackground}" FontSize="11"
              Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="3">
            <MenuItem Header="File">

            </MenuItem>
            <MenuItem Header="Edit">

            </MenuItem>
            <MenuItem Header="Project">

            </MenuItem>
            <MenuItem Header="Window">

            </MenuItem>
            <MenuItem Header="Help">

            </MenuItem>
        </Menu>
        
        <Views:ProjectExplorer Margin="5,5,0,5" Grid.Column="0" Grid.Row="1" />
        
        <GridSplitter Name="MainSplitter" HorizontalAlignment="Right" VerticalAlignment="Stretch" Width="5"
                      Grid.Column="1" Grid.Row="1" Background="Transparent" ResizeBehavior="PreviousAndNext" />

        <TabControl Name="FileEditorTabControl" Style="{StaticResource VS_TabControl}" Margin="0,5,5,5"
                    Grid.Column="2" Grid.Row="1">
        </TabControl>
    </Grid>
</Window>
